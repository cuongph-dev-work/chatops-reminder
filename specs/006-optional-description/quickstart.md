# Quickstart

This feature adds an optional `description` string to reminders, requiring no new environments or dependencies to boot up.

Use the standard Plasmo project startup instructions:

```bash
pnpm install
pnpm dev
```
