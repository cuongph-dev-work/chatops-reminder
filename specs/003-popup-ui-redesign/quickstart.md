# Quickstart: Popup UI Redesign

Since this is purely a frontend popup rework:

1. Ensure the development server is running (`pnpm dev`).
2. Reload the extension in `chrome://extensions`.
3. Click the extension icon in the browser toolbar to open the new popup UI.
4. Switch tabs and test hover actions on individual reminders.
